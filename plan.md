# 当前计划

## 已完成

### 1. 工作流主干已闭环

仓库已经具备从想法到正文、再到状态回填的完整工作流：

- `workflow/00-global.md`：全局规划
- `workflow/01-expand.md`：故事点展开
- `workflow/02-merge.md`：多线交汇处理
- `workflow/03-chapters.md`：章节映射、交接卡、可写性约束
- `workflow/04-draft.md`：单章正文生成与成稿自检
- `workflow/05-update-state.md`：正文写后状态回填

这套工作流已经不是“只有 Prompt 片段”，而是可以实际执行的写作模具。

### 2. 章节规划层已经补强

`workflow/03-chapters.md` 已完成：

- 章节可写性证明
- 场景级字数预算
- 薄场景判废
- 高风险章节加严检查
- 最低可撑体量判断
- 单章装载过满提示
- 整卷实跑的批次备注要求

### 3. 正文生成层已经补强

`workflow/04-draft.md` 已完成：

- 成稿准入闸门
- 场景不足失败判定
- 连续失败暂停规则
- 首章特殊要求
- 连续长跑补充检查
- `3000+` 先润色压缩再复查
- 证据公开代价检查
- 主题句与诗词边界检查

### 4. 状态回填层已经补强

`workflow/05-update-state.md` 已完成：

- 正文准入检查
- 禁止把摘要稿、提纲稿、复盘稿直接回填成状态
- 对手认知 / 误判状态
- 批次级状态账本要求
- 批次账本压缩层

### 5. 已完成的核心测试

当前核心测试矩阵已经完成一轮可用闭环，已完成测试如下：

1. `T01 开局吸引力`：通过  
   验证首章可在前 `300~800` 字内完成主角、现状、冲突、钩子。
2. `T02 种田发家`：通过  
   验证同一“农业 / 工具 / 田间见识改变处境”的核心卖点可换不同兑现角度，不会退化成经营流水账。
3. `T04 科举考试`：通过  
   验证考场、场外势力、文章声名可以交替推进。
4. `T04-upgrade 府试正途压力`：通过  
   验证府试正途压力可通过题纸、删改、批字、复看案等现场动作落地。
5. `T05 朝堂斗争`：通过  
   验证制度压力、多方立场、主角破局可以同时成立。
6. `T06 多线并行`：通过  
   验证多线之间能形成因果交汇，而不是轮流汇报。
7. `T10 阶段收束`：通过  
   验证阶段收束可以做到“半胜”，而不是大结局式清算。
8. `T11 状态回填压力`：通过  
   验证状态回填能承接 6 章连续变化，但也暴露出账本会逐渐变重的问题。
9. `T12 AI 味与合法合规`：部分通过  
   验证整体合规可控，但局部主题句 / 总结句过直，需要工作流补强。

### 6. 已完成的工作流回归修正

#### 1. 由 `T11` 推动的修正

`workflow/05-update-state.md` 已补强：

- 新增批次账本压缩层
- 新增“下一批必读 / 可归档 / 暂停使用 / 疲劳风险与处理建议”
- 明确“下一批必读”不宜超过 8 条

#### 2. 由 `T12` 推动的修正

`workflow/04-draft.md` 已补强：

- 新增主题句与诗词边界检查
- 禁止连续使用主题定义句替代现场推进
- 要求阶段成果优先通过动作、对话、实物或外界反应体现
- 要求诗词 / 古风句服务当前场景
- 若使用现实名篇或明显化用，必须标注来源、改写幅度和场景必要性

---

## 未完成

### 1. 仍未充分覆盖的测试能力

当前还没有充分覆盖：

1. `T03 诗词扬名` 连到身份变化后的长链条
2. `T07 关系推进` 中段复测
3. `T08 失败受挫` 连续失败测试
4. `T09 高信息揭示` 复杂信息测试
5. 更长的 `10~15` 章状态账本长压测
6. 更完整的乡试 / 会试长链条
7. 诗词使用现实名篇或明显化用时的标注流程实测

### 2. 测试记录方式已调整

当前已清理测试样本文本数据，仓库内只保留：

- 工作流规则
- 测试矩阵
- 当前计划中的测试状态结论

不再保留本轮测试的正文 / 章纲 / 评审样本文件。

---

## 当前后续优先级

1. 更长的 `10~15` 章状态账本长压测
2. 更完整的乡试 / 会试长链条
3. `T03 诗词扬名` 连到身份变化后的复测
4. 诗词使用现实名篇或明显化用时的标注流程实测
