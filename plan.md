# 当前计划

## 已完成

### 1. 工作流主干已闭环

仓库已经具备从想法到正文、再到状态回填的完整工作流：

- `workflow/00-global.md`：全局规划
- `workflow/01-expand.md`：故事点展开
- `workflow/02-merge.md`：多线交汇处理
- `workflow/03-chapters.md`：章节映射、交接卡、可写性约束
- `workflow/04-draft.md`：单章正文生成与成稿自检
- `workflow/05-update-state.md`：正文写后状态回填

这套工作流已经不是“只有 Prompt 片段”，而是可以实际执行的写作模具。

### 2. 章节规划层已经补强

`workflow/03-chapters.md` 已完成：

- 章节可写性证明
- 场景级字数预算
- 薄场景判废
- 高风险章节加严检查
- 最低可撑体量判断
- 单章装载过满提示
- 整卷实跑的批次备注要求

### 3. 正文生成层已经补强

`workflow/04-draft.md` 已完成：

- 成稿准入闸门
- 场景不足失败判定
- 连续失败暂停规则
- 首章特殊要求
- 连续长跑补充检查
- `3000+` 先润色压缩再复查

### 4. 状态回填层已经补强

`workflow/05-update-state.md` 已完成：

- 正文准入检查
- 禁止把摘要稿、提纲稿、复盘稿直接回填成状态
- 整卷实跑的批次级状态账本要求

### 5. 已完成的回归验证

- `94` 章已验证可先压缩再判断，`3487` 压到 `2588`
- `95` 章已验证可作为成稿样本
- `91~93` 已完成正文迭代，基本脱离薄稿区
- `94~95` 已形成一组可用的小批次闭环样本

---

## 未完成

### 1. `91~93` 的状态回填还没补齐

正文样本已经推进，但这组三章还缺完整状态账本，需要补成可复查闭环。

### 2. `91~95` 的整段复查还没最终闭合

目前 `94~95` 闭环成立，但 `91~93` 还没和它们串成完整一段。

### 3. 连续小批次闭环还要再验证

还需要继续确认：

- `03 -> 04 -> 05`
- 连续 5~10 章
- 章纲、正文、状态回填、批次复查

### 4. 高风险章型还要继续扩样

还需要继续验证：

- 高密度人物亮相章
- 理念交锋章
- 卷尾高信息密度章
- 多线交汇后的承接章

---

## 继续执行的计划

### 第一步：先补完 `91~93`

优先把这组三章补齐：

1. 状态回填
2. 整组复查
3. 统一判断是否正式转入可用样本

### 第二步：再重做一次 `91~95` 整段复查

确认：

- 章纲能不能继续站住
- 正文能不能稳定成稿
- 状态回填能不能真实支撑下一章

### 第三步：再推进下一组小批次样本

如果 `91~95` 最终稳定，再继续做下一组 3~5 章的连续实跑验证。

### 第四步：最后才进入更长段连续实跑

等小批次稳定后，再考虑更长段推进。当前阶段仍以“把模具跑稳”为先。

