# 2026-07 验证总结

## 已完成

- 草稿与状态任务的瞬时传输失败现在在同一故事单元内按预算恢复；长度、内容、质量与连续性硬失败仍会暂停。
- FileStorage v2 已完成：固定事件/当前快照/检查点/账本文件、事务恢复、调试产物归档、v1 只读兼容及 dry-run 优先迁移。
- 离线验证覆盖 200 章状态重放、10 万事件行、100 次提交阶段中断恢复，以及结构化文件、总文件和单目录数量上限。
- v2 后续分片规划改为使用不晚于目标章节的最新正式快照；避免在未来细纲尚未生成时请求不存在的快照。
- 细纲 Prompt 明确要求每章输出 `writability.is_writable: true`，防止缺少必填布尔字段。

## 真实模型证据

### T21（飞剑驿站，10 章）

- 10/10 正式提交；首稿和最终字数均通过；零正文返工。
- 43 次 API 调用，528,685 Token，平均上下文 17,301 字符。
- 硬失败 0、软告警 0、评分 18/18；`must_read_next` 上限 8；两次状态重建零差异。

### T23（夜灯司，10 章，v2 确认轮）

- 10/10 正式提交；首稿和最终字数均通过；零正文返工。
- 41 次 API 调用，421,263 Token，平均上下文 13,049 字符。
- 硬失败 0、软告警 0、评分 18/18；账本三批均为 8 条；两次状态重建零差异。
- 真实运行中成功通过 5–7 和 8–10 的未来分片规划，验证了 v2 快照修复。

## 未完成与已知诊断

- 40 章连续放行尚未完成，不能宣称发布级长测通过。
- T24 在第 7 章后规划第 8–10 章时，第 9 章模型输出缺少 `writability.is_writable`，Schema 正确暂停并未提交后续章节。该 Prompt 缺口已修复，但用户要求停止新建 T25，故尚未取得修复后的新完整确认轮和 40 章证据。
- 活跃 `runs/`、`.env`、原始 API 载荷和调试压缩包均不纳入版本控制。

## 最后验证

修复 v2 分片快照后，全量离线验证为 196 项通过；随后对 writability Prompt 补强的规划服务回归测试 11 项通过，并通过编译与 `git diff --check`。
