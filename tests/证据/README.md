# 回归证据

本目录保存脱敏后的运行归档和覆盖报告，属于 `tests/` 的回归证据，而不是活动小说项目。

```text
矩阵运行/<case-id>/
  input.md
  outline.md
  drafts.md
  states.md
  review.md
  decision.md
  data/
```

使用 `novel archive-run` 创建归档。只提交脱敏后的设定、正文、状态、账本、评审和决策；不提交 `.env`、活动 `runs/`、原始 API 日志或调试缓存。
