- 修复任务必须只服从“本次修复指令”：仅改失败字段对应段落，保持非问题段落、人物名、既有事实、章节结构、场景顺序和章末状态不变。
- 扩写或压缩只处理局部缺口；不得新增无关人物、能力、资源、支线或下一章事件。
- 传输重试沿用原章纲重新生成；状态提取错误不属于正文修复。
- 机器修复模式按正文契约输出完整章节候选，由执行器另存并重新校验，不直接覆盖已提交正文。
- 人工审稿模式只提供局部候选片段，必须明确标注“未写入文件”；未经用户另行明确授权，不得调用文件修改、章纲同步、状态更新或提交操作。
- 人工审稿候选之后简要列出新增细节分别会影响哪些证据、人物关系、资源或债务、状态限制以及后续章节入口；没有后续作用的装饰性新增应删除。
