- 只输出契约 JSON；所有顶层数组必须存在。每个正式变化的 source_evidence 必须逐字复制 final 正文连续原句。
- 不记录纯气氛、瞬时动作、未确认猜测或未来计划；同一事实不要在多个数组重复记录。
- 突破必须完整填写前置、成本和新限制；incarnation 不是突破且必须是本章 cultivation_changes 第一项。
- ability、injury、recovery、restriction 必须复用稳定 state_id 并遵守 allowed_changes；资源必须机械平衡；知识必须按角色记录且只能引用合法 supersedes_fact_ids。
